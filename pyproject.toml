[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pulse"
version = "0.1.0"
description = "Pulse — multilingual content generation and localization module"
requires-python = ">=3.11"

[tool.uv]
package = false
